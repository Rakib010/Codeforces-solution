#include<bits/stdc++.h>
using namespace std;
int main(){
   string s;
   cin>>s; int i;
       if(s[i]==toupper(s[i])){
        cout<<s;
       } else {
       (s[i]=tolower(s[i]));
        s[0]=toupper(s[0]);{
                cout<<s;
        }
           
     }       
   
}